# Production dependencies for Railway deployment with external backend (Groq / Together AI).
# torch, transformers, peft, bitsandbytes are NOT included — the server runs with
# APEX_SKIP_MODEL_LOAD=1 and delegates all inference to APEX_OPENAI_COMPAT_URL.
fastapi==0.135.2
uvicorn==0.42.0
python-dotenv==1.0.1
httpx>=0.27.0
